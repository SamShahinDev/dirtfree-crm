# Production Environment Variables Template
# Copy this file and fill in actual values
# NEVER commit actual secrets to version control

# =============================================================================
# APPLICATION
# =============================================================================

# Node environment (must be "production")
NODE_ENV=production

# Public app URL (your production domain)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# =============================================================================
# SUPABASE (Production Project)
# =============================================================================

# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Get this from: Supabase Dashboard > Project Settings > API > service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# STRIPE (Live Mode - NOT Test Mode!)
# =============================================================================

# Get these from: Stripe Dashboard > Developers > API Keys (Live mode toggle ON)
STRIPE_SECRET_KEY=sk_live_xxxxxxxxxxxxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxx

# Get this from: Stripe Dashboard > Developers > Webhooks > Signing secret
# After creating webhook endpoint: https://your-domain.com/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# =============================================================================
# TWILIO (Production Account)
# =============================================================================

# Get these from: Twilio Console > Account Dashboard
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token-here

# Your Twilio phone number (production number)
# Format: +1234567890
TWILIO_PHONE_NUMBER=+1234567890

# =============================================================================
# RESEND (Production API)
# =============================================================================

# Get this from: Resend Dashboard > API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Your verified sender email
# Must be verified in Resend dashboard
RESEND_FROM_EMAIL=noreply@your-domain.com

# =============================================================================
# CRON JOBS
# =============================================================================

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Use the SAME secret for all cron job configurations in Vercel
CRON_SECRET=your-secure-random-32-char-hex-string-here

# =============================================================================
# SENTRY (Production Project)
# =============================================================================

# Get these from: Sentry Dashboard > Settings > Projects > dirt-free-crm > Client Keys (DSN)
SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Get this from: Sentry Dashboard > Settings > Account > API > Auth Tokens
SENTRY_AUTH_TOKEN=your-auth-token-here

# Your Sentry organization slug
SENTRY_ORG=your-org-slug

# Your Sentry project slug
SENTRY_PROJECT=dirt-free-crm

# =============================================================================
# OPTIONAL: ADDITIONAL SERVICES
# =============================================================================

# Google Analytics (if using)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Mixpanel (if using)
# NEXT_PUBLIC_MIXPANEL_TOKEN=your-mixpanel-token

# Intercom (if using)
# NEXT_PUBLIC_INTERCOM_APP_ID=your-intercom-app-id

# =============================================================================
# NOTES
# =============================================================================

# 1. NEVER use test/development keys in production
# 2. Rotate all keys after initial setup
# 3. Use strong random strings for CRON_SECRET
# 4. Verify all domains and email addresses
# 5. Test webhooks after configuration
# 6. Keep this template updated as new services are added

# =============================================================================
# SECURITY CHECKLIST
# =============================================================================

# Before deploying, verify:
# [ ] All keys are production (not test/development)
# [ ] CRON_SECRET is strong random string (32+ chars)
# [ ] Webhook secrets are configured
# [ ] Email domain is verified in Resend
# [ ] Phone number is verified in Twilio
# [ ] Stripe is in LIVE mode
# [ ] Sentry is production project
# [ ] No test data in production database
# [ ] All secrets stored in Vercel (not in code)

# =============================================================================
# VERCEL DEPLOYMENT
# =============================================================================

# Add these variables to Vercel:
# Method 1: Vercel CLI
#   vercel env add NEXT_PUBLIC_SUPABASE_URL production
#   (repeat for each variable)

# Method 2: Vercel Dashboard
#   Project Settings > Environment Variables
#   Add each variable for "Production" environment

# IMPORTANT: Redeploy after adding/changing environment variables
#   vercel --prod

# =============================================================================
# ROTATION SCHEDULE
# =============================================================================

# Rotate these secrets regularly for security:
# - CRON_SECRET: Every 90 days
# - STRIPE_SECRET_KEY: Annually or if compromised
# - TWILIO_AUTH_TOKEN: Annually or if compromised
# - RESEND_API_KEY: Annually or if compromised
# - SUPABASE_SERVICE_ROLE_KEY: Only if compromised
# - SENTRY_AUTH_TOKEN: Annually or if compromised

# When rotating:
# 1. Generate new secret in service dashboard
# 2. Update environment variable in Vercel
# 3. Redeploy application
# 4. Verify functionality
# 5. Delete old secret from service
# 6. Document rotation in security log
